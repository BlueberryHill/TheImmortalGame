#pragma once

#include "Vector.h"

namespace CameraUtility
{
	const float DEFAULT_COLLISION_RADIUS = 40.0f;
	const float DEFAULT_SPRING_ARM_LENGTH = 400.0f;
	const float DEFAULT_SPRING_ARM_ROTATION = 25.0f;
	const float DEFAULT_SPRING_ARM_LAG_SPEED = 3.0f;

	const FVector Y_AXIS = FVector(0.0f, 1.0f, 0.0f);
}